import React from "react";

const Contacts = () => {
  return (
    <>
      <div>
        <h2>Contacts</h2>
        <ul>
          {/* {Contacts.map((contact) => (
          <li key={contact.id}>{contact.name}</li>
        ))} */}
        </ul>
      </div>
    </>
  );
};

export default Contacts;
